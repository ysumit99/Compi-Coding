//Incomplete
#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[])
{

	ios_base::sync_with_stdio(false);
    cin.tie(NULL);


	int n,q;
	int y,z;
	char d;
	int ele;

	cin>>n>>q;


	// int type[n]; //,map[200001];

	// std::vector<std::vector<int> > mapping(
    // 200001,
    // std::vector<int>);

	// //memset(map, -1, sizeof(int) * 200001);

	// for (int i = 0; i < n; ++i)
	// {
	// 	cin>>ele;
	// 	type[i] = ele;
	// 	map[ele].push_back = i;
	// }

	

	/*for (int i = 0; i < q; ++i)
	{
		cin>>y>>z>>d;

		ele = map[z];

		if(ele == -1)
			cout<<"-1\n";
		else
		{

			if(d == 'L')
			{
				cout<<(y - ele + n) % n<<"\n";
			}
			else
			{
				cout<<(ele - y + n) % n<<"\n";
			}
		}

	}*/



	return 0;
}